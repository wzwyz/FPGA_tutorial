# 流水线与状态机

在上一章中，我们了解了静态时序分析，并学习了如何阅读vivado时序报告与如何据此修复设计中的时序问题。可以注意到，修复时序违例的一大法宝是打拍，那我们应该如何在设计中有意识地打拍来避免可能的时序违例呢🧐？这就引出了这一章的主题之一：流水线。

# 流水线

假设我们要算出$\sum_{i=1}^{8}a_i$，其中每个$a_i$都是61位的有符号整数。假设我们每周期都能够从外设中获取新的8个值，那能否一周期就直接得出结果？当然是可以的，相应的，由于这些加法运算被堆在了一条时序路径上，这个模块的$f_{max}$将非常有限。

为了让$f_{max}$提高，我们就要尽量的减少一条时序路径上的器件数。于是，我们可以把这些加法运算分散到不同的周期去，得到我们的ver.1加法器模块。

![1](C:\Users\16531\Desktop\FPGA_tutorial\Fig\流水线与状态机\加法器ver.1.png)

虽然理论上来说，ver.1加法器模块每条路径上都只做1次加法，其$f_{max}$应该是ver.0的三倍。但是相应的，ver.1每三个周期才能接收一组新的输入，吞吐量也只有ver.0的$\frac{1}{3}$！那有没有办法在不改变吞吐量的情况下提升$f_{max}$的方法？

FPGA上的器件数量繁多，可以构建成百上千个加法器。为此，我们可以引入三个新的加法器，把ver.1模块在第二周期做的事让给adder5、adder6做，第三周期的事让给adder7来做。这样子，第二周期的时候，adder1~4就有空去处理新进来的数据了。

![2](C:\Users\16531\Desktop\FPGA_tutorial\Fig\流水线与状态机\加法器ver.2.png)

综上，流水线的设计思想是要将一个需要做很长时间的任务，拆成若干个子任务，并设计多个专用模块来处理他们，这样就能兼顾$f_{max}$和模块的吞吐量了。

在流水线的设计中，需要时刻注意每个流水级所依赖的信号。假设信号a被某一流水级同步依赖，但其产生于上上个流水级，因此本流水级应该使用的正确信号是信号a打一拍。一位网友super_star123所写的博文[verilog流水线设计](https://www.cnblogs.com/Dinging006/p/9458988.html "发布于 2018-08-11 21:15")中就犯了这一错误，下面让我们来看看他写的二阶流水全加器：

```Verilog
module adder_4bits_2steps(
    input [7:0] cin_a; 
    input [7:0] cin_b; 
    input cin; 
    input clk; 
    output cout; 
    output [7:0] sum; 
); 
reg cout; 
reg cout_temp; 
reg [7:0] sum; 
reg [3:0] sum_temp; 

always @(posedge clk) begin 
    {cout_temp,sum_temp} <= cin_a[3:0] + cin_b[3:0] + cin; 
end 

always @(posedge clk) begin 
    {cout,sum} <= {{1'b0,cin_a[7:4]} + {1'b0,cin_b[7:4]} + cout_temp, sum_temp}; 
end

endmodule
```

发现了吗？对于第二个流水级（第二个Always块），在其所依赖的信号中，与`cin_a`和`cin_b`对应的`cout_temp`和`sum_temp`会晚于前者一个周期，因此发生了错位！

在发现了这一情况后，这位网友光速将always块中的非阻塞赋值`<=`修改为了阻塞赋值`=`，于是，在行为仿真中，他的输出`sum`是正确的，但是居然在输入信号的下一周期就给出了对应的加和，这显然就有违流水线的初衷了😾！

让我们来看看为什么：在阻塞赋值中，左值将会在赋值后立刻变化，也就是说，第一个always块将于assign块等价！**非阻塞赋值方式和阻塞赋值方式的区别常给设计人员带来问题。问题主要是给"always"块内的寄存器信号的赋值方式不易把握。** 对于初学者，我们还是建议always块内的寄存器型信号都采用非阻塞赋值！

当然这位网友也发现了“一延迟”现象，他为此还特意写了一个四阶流水（假）的全加器来验证他的猜想，最后，甚至评论道：

`emmm，后来才发现这只是功能仿真，看不出来组合逻辑器件的延时效果的~`

🤣👉🤡

值得注意的是，这一现象在初学者身上非常常见，所以遇到想不通问题还是要问问别人，不要硬总结。当然，笔者可能也在不经意间犯了这样的一些问题，大家读这本资料的时候切要小心，不要被带偏了！

# 状态机

在读以上文段的时候，咋一眼看去，能够熟练编程的你可能觉得做到ver.1全加器再简单不过了，但你转念一想，你学过的任何Verilog基础块都不像是能做到顺序执行一系列的操作🤨！Verilog描述的是电路行为，不具备像C语言等运行在计算机上的语言那样具有天然的顺序特性。那我们应该如何实现一个电路，能够在每个周期执行不同的任务？








